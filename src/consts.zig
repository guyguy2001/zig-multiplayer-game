pub const stop_holding_input_threshold = 5;
pub const frame_buffer_size = 3;

// The amount of frames we want our inputs to arrive at the server before it needs them
pub const desired_server_buffer = 3;
